@page "/message"
@attribute [Authorize]
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Redirect legacy /message → /message/inbox (Angular did the same)
        Navigation.NavigateTo("message/inbox", replace: true);
    }
}
