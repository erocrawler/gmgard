@page "/"
@attribute [Authorize]
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Angular had "" → redirect to /title-helper
        Navigation.NavigateTo("title-helper", replace: true);
    }
}
